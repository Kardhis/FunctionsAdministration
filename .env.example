# Copia a .env en la raíz del repo (junto a docker-compose.prod.yml) en el servidor.
# docker compose -f docker-compose.prod.yml carga .env automáticamente.

MYSQL_DATABASE=functions_administration
MYSQL_USER=functions
MYSQL_PASSWORD=cambia_esto
MYSQL_ROOT_PASSWORD=cambia_esto_root
APP_JWT_SECRET=cambia_esto_minimo_32_caracteres_recomendado
