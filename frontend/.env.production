# Cargado con `npm run build` (Vite modo production)
# Vacío = misma origen (puerto 80): nginx debe hacer proxy de /api/ y /auth/ al backend (ver deploy/nginx-api-proxy.snippet.conf).
# Solo usa URL con :8080 si ese puerto está abierto en el firewall hacia Internet.
VITE_API_BASE=
